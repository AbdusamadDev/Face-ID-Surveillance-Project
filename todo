update the camera and criminals details. There will be new criminals or camera may be added, but
criminal details wont be updated as face encodings, and newly added camera wont be connected

results should be displayed correctly to the client, problem is that the data is being
displayed after second glance to the camera

Authorization. Fully should be provided authorization and authentication


continuously check camera life, if camera was disconnected for some reason, then it
should be reconnected, it should check every 1 minute if the camera is connected
if not connected, then it should reconnect that disconnected camera.
